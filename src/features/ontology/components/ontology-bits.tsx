/**
 * Shared atoms — study-notes design language. The class recipes were
 * promoted to shared/ui/wells once a second consumer appeared;
 * re-exported here so ontology imports stay stable.
 */

export { FIELD_WELL, CHIP } from "@/shared/ui/wells";
