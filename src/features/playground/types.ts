/** Sections the playground mirror can show — the real shell's `NavSection`
 *  minus `overview` (the demo has no home page). */
export type PlaygroundSection =
  | "ontology"
  | "knowledge"
  | "skills"
  | "chats"
  | "channels"
  | "members";
