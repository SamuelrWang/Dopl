/** The section heading type: every `SectionPanel` label and the standard dialog title. */
export const SECTION_HEADING_TEXT = "text-display font-semibold text-text-primary";

/** The 14px card-name face (the identity card's name); also card and pane sub-headings. */
export const IDENTITY_NAME_TEXT = "text-title font-medium text-text-primary";
