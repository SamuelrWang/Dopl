/** ⚠ **A RE-EXPORT SINCE 2026-09-17 (wave 2).** Both live in
 *  `src/shared/ui/skeleton-surface.tsx` now, because `src/features/channels/`
 *  wears this wrapper and cannot import `#/` (INVARIANTS §1). */
export { SkeletonSurface, SkeletonChrome } from "@/shared/ui/skeleton-surface";
