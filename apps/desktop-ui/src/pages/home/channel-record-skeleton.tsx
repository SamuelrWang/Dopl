/**
 * ⚠ **A RE-EXPORT SINCE 2026-09-17 (wave 1 item 6, executed in wave 2).** The
 * ghost is `src/features/channels/components/channel-record-skeleton.tsx` now —
 * it is the CHANNEL SURFACE's shape, and the workspace channels page has the same
 * surface to load (F-220). This path survives because `relationship-record.tsx`
 * and `channel-record-skeleton.test.tsx` both name it, and moving a file is not
 * the same change as moving four call sites.
 */
export { ChannelRecordSkeleton } from "@/features/channels/components/channel-record-skeleton";
