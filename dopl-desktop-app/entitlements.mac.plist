<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- Electron requires JIT for V8 -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <!-- Electron requires unsigned executable memory for V8 -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- Electron loads helper frameworks that aren't signed with the same identity -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- Microphone capture for the composer's dictation glyph (2026-09-08).
         ⚠ THE HARDENED RUNTIME IS WHY THIS IS HERE AND NOT ONLY IN Info.plist: with
         `hardenedRuntime: true` macOS refuses audio input to a process that carries no
         audio-input entitlement, and it refuses it WITHOUT PROMPTING — there is no dialog for
         the operator to have missed. `NSMicrophoneUsageDescription` (package.json ›
         build.mac.extendInfo) is the other half: the entitlement makes the ask legal, the usage
         string makes the prompt possible, and either one alone is a silent denial. -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <!-- Allow Apple Events (for opening System Preferences) -->
    <key>com.apple.security.automation.apple-events</key>
    <true/>
  </dict>
</plist>
