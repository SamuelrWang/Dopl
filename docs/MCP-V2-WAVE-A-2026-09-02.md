# MCP / architecture v2 — WAVE A, integrated 2026-09-02

Branch `v2/wave-a`, cut from `integration/mcp-efficiency` at `79b28242`. **Local only — not pushed,
no migration applied anywhere.** 13 slices merged `--no-ff` (every slice's commits kept, no squash),
plus the spec branch, plus the integrator's own leftovers: **62 non-merge commits, 332 files,
+17,442 / −5,500** at `4f742f6d` — ⚠ **the count read 61 and was one short; re-derive rather than
quote:** `git rev-list --no-merges --count 79b28242..<head>`. The REVIEW FIXES below add more.

Spec: `docs/specs/mcp-v2-architecture.md` (on this branch). Wave B is specified there and **not
built** — it needs the rulings listed at the end of this file.

---

## 1. Slices

| Slice | Status | Commits (oldest first) |
|---|---|---|
| architecture (spec) | DONE | `7d0b49e8` |
| **A2** budget gates | DONE | `68ef8872` `788b8d1b` `74ec96ca` |
| **A4** `workspace` arg | DONE | `435529cf` `d64d49ea` |
| **A1** instructions | DONE | `90962eef` `ec97296e` `b377f3cf` `de90c84b` |
| **A5** built-in bound | DONE | `f86a870c` `92ad5178` `48eeb26c` |
| **A8** team axis off the MCP enums | DONE | `38b092ab` `106f76d5` `b42190e4` `e91bd3de` |
| **A3** surface composition | DONE | `89287d29` `4fa7f4d5` `1ec52657` |
| **A7** kinds, residue, drift gate | DONE | `80bcff51` `22437959` `6f301ad4` |
| **A11** acknowledge-shared | DONE | `10dfcc14` `3e4c60c9` `5bd69ba4` `4ead52bb` |
| **A10** idempotency + `maxTurns` | DONE | `edcccf81` `cae570ca` `aaf560da` `8343f4d6` |
| **A12** id resolution | DONE | `0f8fbf9b` `8f9125ba` `e340ce9f` `2bee5d06` |
| **A13** `@dopl/contracts` | DONE | `fe8c3808` `ef8dc464` `37f27674` `4eb8c446` |
| **A6 + A6b** channel seam | DONE | `1d4bb0c0` `c28b270b` `2c3a9789` · `b41e9971` `6d4fa68d` `5b070bc3` |
| **A9** delivery keystone | DONE | `b2d23296` … `de019923` (8) |

**A6 shipped as two slices.** A6 could not delete params — the routing seam (`tools/channel.ts`,
`channel-ops-*.ts`) was in nobody's Owns column (F-428) — so A6b was cut to own it. That is where
the largest single win in the wave came from, and it is worth naming: **the ownership map was the
binding constraint, not the code.**

### Integrator commits

| Commit | What |
|---|---|
| `58492545` | one declaration for `SessionPillState` / `ChannelSessionTelemetry` (A13 × A9 collision) |
| `d088de45` | `chore(dist)` — the wave's single rebuild (F-424 resolution (c)) |
| `cc80c78c` | leftover **2a** — the `delivery=` and `resolved*` renders (A9 × A6b) |
| `77210d68` | leftover **2b** — F-441, `set_visibility` previews its own publish (A3 × A11) |
| `fecf0233` | leftovers **2c–2g** |
| `c3693c98` | the whole surface re-measured, every ratchet lowered |
| `567d834c` | the eighth non-suite gate + §10 stops quoting stale figures (F-422) |
| `25c40496` | dropped the duplicate F-440/F-441 pair A13's re-merge appended |

---

## 2. Measured, before → after

Re-derived at integration through the real `listTools()` / `listResources()` / `getInstructions()`
over the **merged** tree — not by adding up each slice's claim. Slices compound and none could see
the others: `dopl_kb` fell on A8, on A3 and again on A4. **The command, not the number:**
`npx vitest run src/tool-budget.test.ts` in `packages/mcp-server`.

| Metric | Before (spec, 2026-09-02) | After Wave A | Δ |
|---|---:|---:|---:|
| **Served per connection** | 95,174 ch | **54,702** | **−40,472 (−42%)** |
| ↳ `instructions` written | 17,067 | **1,849** | −15,218 |
| ↳ `dopl_channel` schema | 21,778 | **11,371** | −10,407 |
| ↳ `dopl_kb` schema | 5,432 | 4,819 | −613 |
| ↳ `dopl_agent` schema | 4,598 | 3,912 | −686 |
| ↳ `dopl_chats` schema | 4,147 | 3,519 | −628 |
| Tools served | 18 | **13** | −5 |
| `dopl_channel` ops / params | 24 / 37 | **23 / 34** | folded, not renamed |
| Doctrine (**pulled**) | 28,870 | **32,728** | **+3,858 — the trade** |
| Built-in tool schemas / turn | 87,402 ch (~21.9k tok) | ~20,000 (~5k tok) | A5's capture, not re-run here |

⚠ **The doctrine RISE is the design, and it is licensed in writing** at
`tool-budget.test.ts › DOCTRINE_CEILING`. A6/A6b moved standing doctrine out of `.describe()`
(PUSHED on every connection) into a document that is PULLED only by an agent that asks. **A rise of
1 against a fall of 10.5.** Without a separate budget, every future description cut could be
laundered into an unbounded pulled document while the headline number kept improving.

⚠ **The wire figures (turn-1 45.9k tok) were NOT re-measured here.** They need the live SDK capture
against a local capture server, which this integration had no credential for. A5's own measurement
(2026-09-02, `92ad5178`) stands and its wire list is asserted **by name**, which is the assertion
that matters — a token count is a consequence of the list.

**Targets vs. spec §6:** the served-surface target was ~40,300. **We landed 54,702** — the miss is
almost entirely `dopl_channel`'s schema, which the spec projected at ~8,000 for Wave A and which
A6b brought to 11,371. The remaining weight there is the **op set**, and cutting it is B9.

---

## 3. Guardrails ledger — rows closed

The ledger's Half B (prompt-only rules) had 26 NONE/PARTIAL rows. Wave A closed **twelve** — the
sentence is now backed by code, and the code is what is cited.

| # | The claim | The code that now enforces it |
|---|---|---|
| G1 | deletion is app-only, for any role or token | `sessionOnly: true` on nine `DELETE` routes + a census parsing `DELETE_BLOCKED_OPS` (`cc62df13`, pre-wave) |
| G2 | lifecycle kinds refused | prose corrected to name the **credential**; the fence was already right (A6) |
| G3 | a foreign `agent_id` is refused | refuses only on a positive ≤5-min session row — true where it is knowable (A9, `82afff35`) |
| G6 | the machine narrows, never widens | clamped **server-side** at directive creation; `resolved*` non-null (A9, `a4b0a0ca`) |
| G7 | `chain=true` refused when the channel forbids | 400 `CHANNEL_AGENT_CHAIN_FORBIDDEN` (A9) |
| G8 | an unrecognised `model` falls back silently | **echoed, not refused** — `resolvedModel`, rendered on the launch line (A9 + integration `cc80c78c`) |
| G9 | template resolved twice under two visibilities | resolved to an **id** at request time; the name never reaches the network at spawn (A10, `aaf560da`) |
| G10 | do not re-issue on a timeout | `client_msg_id` + partial unique index; a collision returns the existing directive (A10, `edcccf81`) |
| G14 | milestone = one line | capped 240 chars, no newlines, at the routing seam (A6b) |
| G16 | "everyone here will see it" | server precondition `acknowledgeShared` → 400 `CONTAINER_PUBLISH_UNACKNOWLEDGED`, one helper, **all three callers** (A11) + `set_visibility` (F-441, integration) |
| G19 | "loop until the goal is met, then STOP" | `maxTurns` from one constant on every spawn (A10, `cae570ca`) |
| G22 | `dopl_channel` is granted — do not test for it | reworded to "granted **and op-scoped**", which is what `grantDecision` does (A1, `b377f3cf`) |

**Still open, and each is a Wave B ruling, not an oversight:** G4 (deleted by B11, not fenced),
G5 (after B4), G13 (B6), G17 + G18 (B7), **G20 — the one A9 row that did not ship** (F-450), and
G23 (measured, no leak found). G21 shipped as a test (A5, `92ad5178`); G24 shipped as
`@dopl/contracts` (A13).

---

## 4. Rulings owed — Samuel

**Verbatim from the spec (§4 Wave B / §5), unchanged:**

1. **T82** — which sentence comes out of the seven over-cap descriptions, or is the ratchet the answer? *(rec: the ratchet, until the model changes)*
2. **B1** — fan-out narrowing, which reverses ruling 4 of 2026-08-21. *(rec: narrow)*
3. **B6** — SOLO / TRIAGE tier deletion. *(rec: delete TRIAGE; keep SOLO as a server verdict)*
4. **B8** — `channel_pings` fold vs. fourth lane. *(rec: fold; **do not apply** `20260907130000`)* — **due now**, because applying it creates data that makes the fold expensive.
5. **B10** — `kind='personal'` container + `home_scoped` drop. *(rec: yes, dual-written over one release)*
6. **B2** — `workspace=` off non-list ops. *(rec: yes, after A12 — which has landed)*
7. **B11** — copy ops removed in favour of grants, and with them whether F-419 is fenced or deleted. *(rec: yes, last)*
8. **B5** — RLS as the fence vs. deleting the inert policies. *(rec: RLS as the fence, on a schedule; gate the pair meanwhile; never delete)*
9. **B7** — `Bash` under `full`. *(rec: a fourth `channel_agent` profile, not a removal)*
10. **B9** — retiring the old channel ops, 24 → 5. *(rec: ship additive, retire on a deprecation window)*
11. **B3** — which roles lose which tools. *(rec: courier first — A3 shipped the mechanism, so this is one row)*
12. **B4** — team axis dropped in the DB and the app. *(rec: yes)*
13. **A5 default TAKEN:** `Agent` and `Skill` come off `full`. One line reverses it.
14. **A6 default TAKEN:** `op="help"` is **sectioned, not deleted**.
15. **A12 / C2 default TAKEN:** the auto-target count is **not** narrowed.
16. **X0** — the codex/cursor adapter lanes, owed since 2026-09-01. Ask **before B3 ships**.
17. **The four F-417 defaults** stand unless loosened.

**New, raised by the slices themselves:**

18. **A12 — a personal-shelf template now LAUNCHES into a container, with no copy.** `readTemplateById`
    follows an id into the container the id names. ⚠ **And the two lanes of one launch now disagree**:
    the desktop's spawn-time `/resolve` resolves such an id, while `resolveTemplateForDirective`
    (the `launch_agent` ref lane) is still workspace-keyed and 404s it. It fails **closed** on the
    refusing side and there is still exactly **one read door** for launch content, so this is a scope
    question, not a second fence. Recorded at the seam in `service-launch-template.ts`. One line
    either way.
19. **A9 — G8 echoes rather than refuses.** An unrecognised `model` still reaches the machine; the
    caller is now *told* it was unrecognised. The alternative is a refusal that would break a newer
    desktop running a model this server predates.
20. **F-449 — the ceiling `channels.agent_*` records has NO editing surface**, so the server's clamp
    is armed and unarmed: every column is null, and null means "no ceiling recorded". Fail-open by
    construction and deliberately so — an inert clamp is reversible; a *promise* of one is the class
    G6 exists to record. It needs a Settings control on the channel, manage-gated.
21. **F-450 — G20 ("channel work is answered into the channel") did not ship.** It is an eighth
    session-health field and was out of A9's scope.

---

## 5. Migrations — none applied

Strictly increasing, all SEVEN unapplied, all idempotent (`ADD COLUMN IF NOT EXISTS`;
`DROP CONSTRAINT IF EXISTS` before every `ADD CONSTRAINT`):

| Version | File | From |
|---|---|---|
| 20260907130000 | `channel_pings` | pre-wave — ⚠ **B8 says do not apply** |
| 20260908120000 | `knowledge_pinned_startup_context` | pre-wave |
| 20260909120000 | `channel_sessions_health` | pre-wave |
| 20260910120000 | `channel_launch_directives_posture` | pre-wave |
| **20260911120000** | `launch_direction_client_msg_id` | **A10** |
| **20260912120000** | `channel_delivery_verdict` | **A9** |
| **20260913120000** | `channel_tasks_author_scoped_idempotency` | **review C14 / D1** |

**Replay SKIPPED: Docker is down on this machine** (`docker info` fails), so `supabase start` cannot
run and no local replay was possible. That is a measurement, not a claim — re-run it before applying.
⚠ `supabase migration list` prints VERSIONS while every doc cites FILENAMES; join on the name.

---

## 6. What Samuel has to do

1. **Take the rulings above.** Nos. 4 (B8) and 16 (X0) are the two that block other work.
2. **Apply the migrations, in order**, once B8 is decided — 20260907130000 is the one that may never
   be applied. ⚠ **THE ROLLBACK SENTENCE HERE WAS WRONG AND IS CORRECTED (2026-09-02, review).**
   It said the new migrations roll back with a `DROP` on a column nothing reads. Per file:
   - `20260911120000` creates a partial unique index on **two** tables and adds a column to each.
     Rollback is a `DROP INDEX` then a `DROP COLUMN` **and the CODE revert must land with it** —
     a create passing `client_msg_id` to a table without the column is a PostgREST 400. That
     file's own header says so.
   - `20260912120000` also **DROPs and re-creates `channel_message_insert`** with four new
     parameters. Rolling that back is not a `DROP COLUMN`: the eight-argument function is GONE
     and has to be re-created, or every post 404s on the RPC.
   - `20260913120000` **widens** an index, so reverting it can FAIL on data written in the
     meantime — two members who each opened a thread under one key are legal now and are not
     legal under the pair index. That is a data question, not a DDL question.
3. **Push `v2/wave-a` for CI.** Nothing here has run on Linux or Windows. ⚠ The wave adds an eighth
   non-suite gate (the committed-`dist` check) that has never run in CI — it passes locally, and a
   gate's first CI run is where lockfile and platform differences show up.
4. **Decide no. 18** — the shelf-template launch behaviour A12 changed by generalising the read.

---

## 7. Gates, at `567d834c` (pre-review)

| Gate | Result |
|---|---|
| root suite (`npm test`) | **359 files / 5,357 tests, 0 fail** |
| `@dopl/client` | 4 / 58, 0 fail |
| `@dopl/mcp-server` | 90 / 1,239, 0 fail |
| `@dopl/desktop-ui` | 46 / 433, 0 fail |
| desktop `node --test` | **3,007 pass, 0 fail** |
| root lint `--max-warnings 0` | clean |
| desktop lint | clean |
| root typecheck | clean |
| `typecheck -w @dopl/desktop-ui` | clean |
| `check-doc-refs` | OK |
| `size-check` (500-line cap) | all within cap |
| the six drift scripts | all ✅ |
| canonical-sets census | 29 / 29 |
| committed-`dist` check | clean after one `build:packages` |

**No flake was hit.** The five known flakes (credits/consume, `shared/version/latest-release`,
`login-form-core`, `agent-authoring`, `billing/credits-link-reroute`) all passed first time in the
full run, so none needed isolation.

### 7b. Gates, re-run whole after the review fixes (2026-09-02)

| Gate | Result |
|---|---|
| root suite (`npm test`) | **361 files / 5,395 tests, 0 fail** |
| `@dopl/client` | 4 / 58, 0 fail |
| `@dopl/mcp-server` | **91 / 1,249**, 0 fail |
| `@dopl/desktop-ui` | 46 / 433, 0 fail |
| desktop `node --test` | **3,012 pass, 0 fail** |
| root lint `--max-warnings 0` | clean |
| desktop lint | clean |
| root typecheck | clean |
| `typecheck -w @dopl/desktop-ui` | clean |
| `check-doc-refs` | OK |
| `size-check` (500-line cap) | all within cap |
| the five drift scripts | all ✅ |
| canonical-sets census | 29 / 29 |
| committed-`dist` check | clean after one `build:packages` |

⚠ **ONE KNOWN FLAKE SURFACED AND IT IS NOT A FAILURE.**
`billing/credits-link-reroute` raised `EnvironmentTeardownError: Closing rpc while
"onUserConsoleLog" was pending` — an unhandled rejection, not a failed assertion; all 14 of its
cases pass. It **reproduces on a CLEAN `4f742f6d` tree in isolation**, so it is pre-existing and
belongs to the five above.

⚠ **AND A SIXTH FLAKE WAS FOUND, filed as F-454.**
`service-launch-agent.test.ts`'s TTL case reads `Date.now()` before the service reads its own, so
its zero-slack upper bound fails on any millisecond that elapses between the two. It failed once
in a scoped run and passed on isolation. **It fails on machine SPEED rather than ordering, so it
will show up more often in CI than locally.** Left unfixed: outside the 26, and the fix is to
inject the clock rather than to widen the bound (widening it would stop the assertion catching a
second, longer TTL, which is what it exists to pin).

---

## 8. Review fixes — 2026-09-02, over `4f742f6d`

An Opus review of the integrated branch filed **26 findings** (2 BLOCKER, 7 MAJOR,
15 MINOR, 2 NIT) plus four decisions the Desktop Agent took for the fix agent.
**All 26 are addressed; none is contested.** Every fence below is backed by a test
that fails when the fix is reverted — the standing complaint about this wave's own
guardrails ledger, applied to its repair.

⚠ **THE FOUR DECISIONS ARE DESKTOP AGENT DEFAULTS AND SAMUEL MAY REVERSE ANY OF
THEM.** Three landed as written. **D4 landed in part, and the part that did not is
recorded rather than glossed** — see its row.

| # | Decision | Taken as | Status |
|---|---|---|---|
| **D1** | widen `channel_tasks` idempotency to per-author, on `20260822120000`'s shape; collapse the schema sentence | new migration `20260913120000`, `findOwnTaskByClientId`, one describe | **as written** — Desktop Agent default, Samuel may reverse |
| **D2** | wire skills into `assertSharedPublishAcknowledged` (both write paths + `set_visibility`) | server fence on create+update, `acknowledgeShared` on both bodies, 400 mapped, `opSetVisibility` previews | **as written** — Desktop Agent default, Samuel may reverse |
| **D3** | bind the delivery ack to a session the caller owns | `sessionKey` required on the ack, checked against the set that same push reconciled | **as written** — Desktop Agent default, Samuel may reverse |
| **D4** | write the ceiling into `resolved_*` when an axis was not asked (never null); **add NOT NULL/CHECK** if the columns are new enough; else correct the ledger honestly. *Prefer non-null* | the ceiling IS written now — and **no constraint** | **PART TAKEN, PART DECLINED, and the decline is measured** |

**D4's declined half, with the measurement.** `NULL` on `resolved_*` now means
exactly one thing: *no ceiling is recorded on that axis*. That is **every channel
today** — `channels.agent_tool_ceiling` / `agent_message_ceiling` /
`agent_chain_allowed` have no editing surface at all (F-449, this wave's own
finding), so a `NOT NULL` would 500 every launch rather than record anything. It
would also contradict `20260912120000`'s written rollback argument, in the same
wave. So "never null" is true wherever a ceiling exists and cannot be made true
anywhere else; the migration header and INVARIANTS §say that instead of claiming a
non-null the schema cannot hold. **One Settings control on the channel (F-449)
turns this into a constraint that would hold.**

### The 26, by severity

| Finding | Commit |
|---|---|
| **BLOCKER** `channel-schema.ts` — C14 not delivered | `36291f41` |
| **BLOCKER** `skills/server/service-writes.ts` — G16 closed for 2 of 3 types | `348c9fdd` |
| **MAJOR** `service-writes-delivery.ts` — ack fence was membership only (D3) | `28b8e796` |
| **MAJOR** `lib/agent-models.ts` — unguarded prototype lookup on caller text | `18d85faf` |
| **MAJOR** `service-wake-verdict.ts` — every non-message row stamped `unreachable` | `248cbd36` |
| **MAJOR** `20260912120000` + `service-launch.ts` — G6's non-null was false (D4) | `12aebea3` |
| **MAJOR** `agent-ops-write.ts` + `knowledge-ops-write.ts` — the 400 → re-preview loop | `b4d96fce` |
| **MAJOR** `parity.test.ts` — `READ_OPS` kept a deleted op, and had no ⊆ assertion | `f4e8ec9a` |
| **MAJOR** `docs/ENGINEERING.md` — "14 tools", a deleted constant, `get_thread` live | `f4e8ec9a` |
| **MINOR** `agent-templates/schema.ts` — A8 was an MCP enum, not a fence | `99a95223` |
| **MINOR** `schema-launch.ts` — the message axis clamped by index, and widened | `cfc76eca` |
| **MINOR** `service-writes-delivery.ts` — no `workspace_id` term on the stamp | `28b8e796` |
| **MINOR** wave doc / spec — A13 "imported by nothing yet" | this section + spec A13 row |
| **MINOR** `packages/mcp-server/package.json` — phantom `@dopl/contracts` | `dca2faa6` |
| **MINOR** spec G19 — `maxTurns` "from the launch posture"; it shipped fixed | `59347ab2` |
| **MINOR** `repository-launch.ts` — the idempotency predicate had no test | `59347ab2` |
| **MINOR** `channel-doctrine.ts` — still taught `op="post"` with `kind` | `aadfd74b` |
| **MINOR** `channel.ts` — `op="pings"` silently dropped `since` | `aadfd74b` |
| **MINOR** `resolve-resource.ts` — `caller.userId` raw in an `.or()` filter | `fc2f23af` |
| **MINOR** `tool-profile-header.ts` — duplicate headers silently un-narrowed | `fc2f23af` |
| **MINOR** `main/delivery-ack.js` — strengthen-only was conditional on the operator | `28b8e796` |
| **MINOR** `service-writes-delivery.test.ts` — vacuous (`.slice(0, 3)` dropped the filter) | `28b8e796` |
| **NIT** `test/launch-max-turns.test.mjs` — a regex over module source | `59347ab2` |
| **NIT** `instructions-budget.test.ts` — "written IS delivered" restated the cap | `59347ab2` |
| **NIT** `main/runtime/claude/tools.js` — spec A5 lists `NotebookEdit` as removed | `59347ab2` |
| **Also** the wave doc said 61 non-merge commits; it is 62 | this section |

### Two the review located in the wrong file, and one it half-called

- **A13's "imported by nothing yet"** is in `docs/specs/mcp-v2-architecture.md`,
  not in §1 of this file. Corrected where it lives; the claim was indeed stale.
- **The rollback claim** it flagged is §6 item 2 of THIS file and was wrong in
  three separate ways; §6 now states each.
- **`OWN_CHANNEL_READ_OPS` still lists `get_thread`** (`main/session-profiles.js`),
  and it is NOT removed. The desktop ships on its own cadence and can face a
  server, or a cached connector schema, still publishing the op; an ALLOW entry
  that outlives its op fails SAFE — the same argument `RETIRED_DOPL_TOOLS` makes
  for its DENY entries. ENGINEERING.md says so rather than the code changing.

### Ratchets re-measured

Every ceiling `tool-budget.test.ts` holds was re-derived over the fixed tree, and
each moved one is banked in the commit that moved it.

| Ratchet | Was | Now | Why |
|---|---:|---:|---|
| `dopl_channel` schema | 11,371 | **11,333** | `client_msg_id` collapsed to one sentence (C14) |
| `dopl_skill` description | 1,586 | **1,580** | the `set_visibility` op line lost a duplicated permission clause |
| `dopl_skill` schema | 2,800 | **3,059** | ⚠ **A RISE, LICENSED** — `confirm_token`, a FENCE. A required argument cannot move into a pulled document, and trimming its description into uselessness would buy the number by disarming the preview |
| `dopl_agent` schema | 3,912 | **3,929** | the `visibility` describe states the default the wire now sends |
| served per connection | 54,702 | **54,934** | the two above, net |
| pulled doctrine | 32,728 | **32,551** | one idempotency rule instead of two; the deleted `kind` param unnamed |

⚠ **THE +232 IS TWO FENCES AND ONE CLARIFIED DEFAULT.** Doctrine fell 177 in the
same pass. A fence costs served characters and is worth them; prose is what these
ceilings exist to refuse, and the distinction is the only thing that keeps a
ratchet honest.

### What Samuel still owes here

1. **F-449 — a Settings control for the channel ceiling.** It is now the ONLY
   reason `resolved_*` can be null, so it is also the only thing standing between
   this wave and the constraint D4 asked for.
2. **The rulings in §4 are untouched.** No fix above takes one.
3. **Replay is still owed on all seven migrations** — Docker is still down on this
   machine (§5). `20260913120000` has executed nowhere.

---

## 9. Slice A14 — the description style pass (2026-09-02, on top of §8)

⚠ **§2's table is Wave A's and is now two waves stale.** Re-derive with
`npx vitest run src/tool-budget.test.ts` in `packages/mcp-server`, which prints
every figure and fails on a drifted one in either direction.

| Ratchet | Was (§8) | Now | Why |
|---|---:|---:|---|
| served per connection | 54,934 | **51,996** | −2,938 |
| ↳ all 13 descriptions | 20,558 | **16,092** | −22%: a description carries nothing its args already say |
| ↳ all 13 schemas | 32,527 | **34,053** | ⚠ **+1,526, ALL of it response-size knobs** — see below |
| ↳ `instructions` | 1,849 | **1,851** | the contract lost the "where to find your id" paragraph; the block that replaced it GIVES the id |
| `dopl_channel` schema | 11,333 | **11,609** | ⚠ +276, `response_format` |
| pulled doctrine | 32,551 | **32,551** | untouched |

⚠ **THE SCHEMA RISE IS THE ONE ON THIS SURFACE THAT PAYS PER CALL.** Every other
figure here is a fixed cost per CONNECTION. These 1,526 chars buy a recurring
saving per RESULT: `concise` drops ~750 chars of scope note from every
`dopl_search`, a two-line legend from every `dopl_status` (the call an
orchestrator makes most), and the timestamp plus session tag from every message
of every `read`. Licensed on `dopl_skill`'s `confirm_token` precedent — a
published argument cannot move into a pulled document.

**What shipped:** one rendered house style for all thirteen descriptions
(`tools/tool-style.ts`, which THROWS at import rather than trimming); named
error codes an agent can string-match on the wire (`tools/tool-errors.ts`); an
untrusted-content FENCE with a per-response random suffix (`tools/untrusted-fence.ts`);
three response-size knobs applied in the renderers, so no route or contract
changed (`tools/response-size.ts`); per-connection identity in the briefing
(`instructions.ts › ConnectionIdentity`). The audit is
`docs/MCP-TOOL-DESCRIPTION-CHECKLIST.md`; the shape gate is
`tools/tool-style.test.ts`.

**F-454 is resolved** (the zero-slack TTL flake), and its own entry records that
the finding's proposed remedy assumed an injection point that does not exist.

### What A14 leaves open

1. **Three `Q7` FIXes**, named in the checklist: `dopl_members` has no `fields=`,
   `dopl_ontology` no `response_format`, `dopl_agent(op="get")` no `max_chars`.
2. **`dopl_channel` renders no `Limits:` block**, and it is the one place the
   one-source-for-every-number rule is not yet true: `summary` states the
   route's 200 where the schema publishes `maxLength: 2000`, so rendering would
   push two numbers for one field. ⚠ **THIS NEEDS A RULING, NOT A TIDY.** The
   looser cap is PINNED by two suites that exist to stop exactly this edit —
   `packages/mcp-server/src/tools/channel-schema-caps.test.ts › keeps \`summary\` at the LOOSER 2000`
   (*"Pinned below so a 'consistency' pass cannot tighten it"*) and
   `packages/mcp-server/src/tools/channel-post-guidance.test.ts` — on the argument that an over-length
   summary should be the ROUTE's to reject, by name, rather than an opaque
   client-side `-32602`. Their own comments concede the cap's original owner
   (the close summary) is gone, so the only live argument is the error quality.
   Decide that, then reconcile the cap and let the renderer publish it.
3. **Two prose budgets sit above the 1,200 cap** (`KB_PROSE_BUDGET`,
   `AGENT_PROSE_BUDGET`) and in both the excess is the fence. The honest way to
   buy it back is a pulled doctrine resource per tool, which is B9's shape.
4. **Two `ConnectionIdentity` fields reach no connection.** `liveAgents` and
   `posture` are declared on `BootOptions` and rendered by `instructions.ts`,
   but the only caller — `src/app/api/mcp/route.ts` — passes neither, and
   neither can be learned at boot without the loopback `factory.ts › bootServer`
   forbids. So a real connection always renders *"your live agents: dopl_status"*
   and never a posture, which is the honest degradation the block was designed
   for and is NOT the round-trip deletion the slice claimed for those two lines.
   `userId`, the default workspace, the home-channel count and `boundChannelId`
   all do render. Supplying the two is the desktop transport's work, not this
   server's.
