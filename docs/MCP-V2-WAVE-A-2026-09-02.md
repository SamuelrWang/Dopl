# MCP / architecture v2 — WAVE A, integrated 2026-09-02

Branch `v2/wave-a`, cut from `integration/mcp-efficiency` at `79b28242`. **Local only — not pushed,
no migration applied anywhere.** 13 slices merged `--no-ff` (every slice's commits kept, no squash),
plus the spec branch, plus the integrator's own leftovers: **61 non-merge commits, 332 files,
+17,442 / −5,500.**

Spec: `docs/specs/mcp-v2-architecture.md` (on this branch). Wave B is specified there and **not
built** — it needs the rulings listed at the end of this file.

---

## 1. Slices

| Slice | Status | Commits (oldest first) |
|---|---|---|
| architecture (spec) | DONE | `7d0b49e8` |
| **A2** budget gates | DONE | `68ef8872` `788b8d1b` `74ec96ca` |
| **A4** `workspace` arg | DONE | `435529cf` `d64d49ea` |
| **A1** instructions | DONE | `90962eef` `ec97296e` `b377f3cf` `de90c84b` |
| **A5** built-in bound | DONE | `f86a870c` `92ad5178` `48eeb26c` |
| **A8** team axis off the MCP enums | DONE | `38b092ab` `106f76d5` `b42190e4` `e91bd3de` |
| **A3** surface composition | DONE | `89287d29` `4fa7f4d5` `1ec52657` |
| **A7** kinds, residue, drift gate | DONE | `80bcff51` `22437959` `6f301ad4` |
| **A11** acknowledge-shared | DONE | `10dfcc14` `3e4c60c9` `5bd69ba4` `4ead52bb` |
| **A10** idempotency + `maxTurns` | DONE | `edcccf81` `cae570ca` `aaf560da` `8343f4d6` |
| **A12** id resolution | DONE | `0f8fbf9b` `8f9125ba` `e340ce9f` `2bee5d06` |
| **A13** `@dopl/contracts` | DONE | `fe8c3808` `ef8dc464` `37f27674` `4eb8c446` |
| **A6 + A6b** channel seam | DONE | `1d4bb0c0` `c28b270b` `2c3a9789` · `b41e9971` `6d4fa68d` `5b070bc3` |
| **A9** delivery keystone | DONE | `b2d23296` … `de019923` (8) |

**A6 shipped as two slices.** A6 could not delete params — the routing seam (`tools/channel.ts`,
`channel-ops-*.ts`) was in nobody's Owns column (F-428) — so A6b was cut to own it. That is where
the largest single win in the wave came from, and it is worth naming: **the ownership map was the
binding constraint, not the code.**

### Integrator commits

| Commit | What |
|---|---|
| `58492545` | one declaration for `SessionPillState` / `ChannelSessionTelemetry` (A13 × A9 collision) |
| `d088de45` | `chore(dist)` — the wave's single rebuild (F-424 resolution (c)) |
| `cc80c78c` | leftover **2a** — the `delivery=` and `resolved*` renders (A9 × A6b) |
| `77210d68` | leftover **2b** — F-441, `set_visibility` previews its own publish (A3 × A11) |
| `fecf0233` | leftovers **2c–2g** |
| `c3693c98` | the whole surface re-measured, every ratchet lowered |
| `567d834c` | the eighth non-suite gate + §10 stops quoting stale figures (F-422) |
| `25c40496` | dropped the duplicate F-440/F-441 pair A13's re-merge appended |

---

## 2. Measured, before → after

Re-derived at integration through the real `listTools()` / `listResources()` / `getInstructions()`
over the **merged** tree — not by adding up each slice's claim. Slices compound and none could see
the others: `dopl_kb` fell on A8, on A3 and again on A4. **The command, not the number:**
`npx vitest run src/tool-budget.test.ts` in `packages/mcp-server`.

| Metric | Before (spec, 2026-09-02) | After Wave A | Δ |
|---|---:|---:|---:|
| **Served per connection** | 95,174 ch | **54,702** | **−40,472 (−42%)** |
| ↳ `instructions` written | 17,067 | **1,849** | −15,218 |
| ↳ `dopl_channel` schema | 21,778 | **11,371** | −10,407 |
| ↳ `dopl_kb` schema | 5,432 | 4,819 | −613 |
| ↳ `dopl_agent` schema | 4,598 | 3,912 | −686 |
| ↳ `dopl_chats` schema | 4,147 | 3,519 | −628 |
| Tools served | 18 | **13** | −5 |
| `dopl_channel` ops / params | 24 / 37 | **23 / 34** | folded, not renamed |
| Doctrine (**pulled**) | 28,870 | **32,728** | **+3,858 — the trade** |
| Built-in tool schemas / turn | 87,402 ch (~21.9k tok) | ~20,000 (~5k tok) | A5's capture, not re-run here |

⚠ **The doctrine RISE is the design, and it is licensed in writing** at
`tool-budget.test.ts › DOCTRINE_CEILING`. A6/A6b moved standing doctrine out of `.describe()`
(PUSHED on every connection) into a document that is PULLED only by an agent that asks. **A rise of
1 against a fall of 10.5.** Without a separate budget, every future description cut could be
laundered into an unbounded pulled document while the headline number kept improving.

⚠ **The wire figures (turn-1 45.9k tok) were NOT re-measured here.** They need the live SDK capture
against a local capture server, which this integration had no credential for. A5's own measurement
(2026-09-02, `92ad5178`) stands and its wire list is asserted **by name**, which is the assertion
that matters — a token count is a consequence of the list.

**Targets vs. spec §6:** the served-surface target was ~40,300. **We landed 54,702** — the miss is
almost entirely `dopl_channel`'s schema, which the spec projected at ~8,000 for Wave A and which
A6b brought to 11,371. The remaining weight there is the **op set**, and cutting it is B9.

---

## 3. Guardrails ledger — rows closed

The ledger's Half B (prompt-only rules) had 26 NONE/PARTIAL rows. Wave A closed **twelve** — the
sentence is now backed by code, and the code is what is cited.

| # | The claim | The code that now enforces it |
|---|---|---|
| G1 | deletion is app-only, for any role or token | `sessionOnly: true` on nine `DELETE` routes + a census parsing `DELETE_BLOCKED_OPS` (`cc62df13`, pre-wave) |
| G2 | lifecycle kinds refused | prose corrected to name the **credential**; the fence was already right (A6) |
| G3 | a foreign `agent_id` is refused | refuses only on a positive ≤5-min session row — true where it is knowable (A9, `82afff35`) |
| G6 | the machine narrows, never widens | clamped **server-side** at directive creation; `resolved*` non-null (A9, `a4b0a0ca`) |
| G7 | `chain=true` refused when the channel forbids | 400 `CHANNEL_AGENT_CHAIN_FORBIDDEN` (A9) |
| G8 | an unrecognised `model` falls back silently | **echoed, not refused** — `resolvedModel`, rendered on the launch line (A9 + integration `cc80c78c`) |
| G9 | template resolved twice under two visibilities | resolved to an **id** at request time; the name never reaches the network at spawn (A10, `aaf560da`) |
| G10 | do not re-issue on a timeout | `client_msg_id` + partial unique index; a collision returns the existing directive (A10, `edcccf81`) |
| G14 | milestone = one line | capped 240 chars, no newlines, at the routing seam (A6b) |
| G16 | "everyone here will see it" | server precondition `acknowledgeShared` → 400 `CONTAINER_PUBLISH_UNACKNOWLEDGED`, one helper, **all three callers** (A11) + `set_visibility` (F-441, integration) |
| G19 | "loop until the goal is met, then STOP" | `maxTurns` from one constant on every spawn (A10, `cae570ca`) |
| G22 | `dopl_channel` is granted — do not test for it | reworded to "granted **and op-scoped**", which is what `grantDecision` does (A1, `b377f3cf`) |

**Still open, and each is a Wave B ruling, not an oversight:** G4 (deleted by B11, not fenced),
G5 (after B4), G13 (B6), G17 + G18 (B7), **G20 — the one A9 row that did not ship** (F-450), and
G23 (measured, no leak found). G21 shipped as a test (A5, `92ad5178`); G24 shipped as
`@dopl/contracts` (A13).

---

## 4. Rulings owed — Samuel

**Verbatim from the spec (§4 Wave B / §5), unchanged:**

1. **T82** — which sentence comes out of the seven over-cap descriptions, or is the ratchet the answer? *(rec: the ratchet, until the model changes)*
2. **B1** — fan-out narrowing, which reverses ruling 4 of 2026-08-21. *(rec: narrow)*
3. **B6** — SOLO / TRIAGE tier deletion. *(rec: delete TRIAGE; keep SOLO as a server verdict)*
4. **B8** — `channel_pings` fold vs. fourth lane. *(rec: fold; **do not apply** `20260907130000`)* — **due now**, because applying it creates data that makes the fold expensive.
5. **B10** — `kind='personal'` container + `home_scoped` drop. *(rec: yes, dual-written over one release)*
6. **B2** — `workspace=` off non-list ops. *(rec: yes, after A12 — which has landed)*
7. **B11** — copy ops removed in favour of grants, and with them whether F-419 is fenced or deleted. *(rec: yes, last)*
8. **B5** — RLS as the fence vs. deleting the inert policies. *(rec: RLS as the fence, on a schedule; gate the pair meanwhile; never delete)*
9. **B7** — `Bash` under `full`. *(rec: a fourth `channel_agent` profile, not a removal)*
10. **B9** — retiring the old channel ops, 24 → 5. *(rec: ship additive, retire on a deprecation window)*
11. **B3** — which roles lose which tools. *(rec: courier first — A3 shipped the mechanism, so this is one row)*
12. **B4** — team axis dropped in the DB and the app. *(rec: yes)*
13. **A5 default TAKEN:** `Agent` and `Skill` come off `full`. One line reverses it.
14. **A6 default TAKEN:** `op="help"` is **sectioned, not deleted**.
15. **A12 / C2 default TAKEN:** the auto-target count is **not** narrowed.
16. **X0** — the codex/cursor adapter lanes, owed since 2026-09-01. Ask **before B3 ships**.
17. **The four F-417 defaults** stand unless loosened.

**New, raised by the slices themselves:**

18. **A12 — a personal-shelf template now LAUNCHES into a container, with no copy.** `readTemplateById`
    follows an id into the container the id names. ⚠ **And the two lanes of one launch now disagree**:
    the desktop's spawn-time `/resolve` resolves such an id, while `resolveTemplateForDirective`
    (the `launch_agent` ref lane) is still workspace-keyed and 404s it. It fails **closed** on the
    refusing side and there is still exactly **one read door** for launch content, so this is a scope
    question, not a second fence. Recorded at the seam in `service-launch-template.ts`. One line
    either way.
19. **A9 — G8 echoes rather than refuses.** An unrecognised `model` still reaches the machine; the
    caller is now *told* it was unrecognised. The alternative is a refusal that would break a newer
    desktop running a model this server predates.
20. **F-449 — the ceiling `channels.agent_*` records has NO editing surface**, so the server's clamp
    is armed and unarmed: every column is null, and null means "no ceiling recorded". Fail-open by
    construction and deliberately so — an inert clamp is reversible; a *promise* of one is the class
    G6 exists to record. It needs a Settings control on the channel, manage-gated.
21. **F-450 — G20 ("channel work is answered into the channel") did not ship.** It is an eighth
    session-health field and was out of A9's scope.

---

## 5. Migrations — none applied

Strictly increasing, all six unapplied, all idempotent (`ADD COLUMN IF NOT EXISTS`;
`DROP CONSTRAINT IF EXISTS` before every `ADD CONSTRAINT`):

| Version | File | From |
|---|---|---|
| 20260907130000 | `channel_pings` | pre-wave — ⚠ **B8 says do not apply** |
| 20260908120000 | `knowledge_pinned_startup_context` | pre-wave |
| 20260909120000 | `channel_sessions_health` | pre-wave |
| 20260910120000 | `channel_launch_directives_posture` | pre-wave |
| **20260911120000** | `launch_direction_client_msg_id` | **A10** |
| **20260912120000** | `channel_delivery_verdict` | **A9** |

**Replay SKIPPED: Docker is down on this machine** (`docker info` fails), so `supabase start` cannot
run and no local replay was possible. That is a measurement, not a claim — re-run it before applying.
⚠ `supabase migration list` prints VERSIONS while every doc cites FILENAMES; join on the name.

---

## 6. What Samuel has to do

1. **Take the rulings above.** Nos. 4 (B8) and 16 (X0) are the two that block other work.
2. **Apply the migrations, in order**, once B8 is decided — 20260907130000 is the one that may never
   be applied. The two new ones are additive columns and a partial unique index, so rollback is a
   `DROP` on a column nothing reads.
3. **Push `v2/wave-a` for CI.** Nothing here has run on Linux or Windows. ⚠ The wave adds an eighth
   non-suite gate (the committed-`dist` check) that has never run in CI — it passes locally, and a
   gate's first CI run is where lockfile and platform differences show up.
4. **Decide no. 18** — the shelf-template launch behaviour A12 changed by generalising the read.

---

## 7. Gates, at `567d834c`

| Gate | Result |
|---|---|
| root suite (`npm test`) | **359 files / 5,357 tests, 0 fail** |
| `@dopl/client` | 4 / 58, 0 fail |
| `@dopl/mcp-server` | 90 / 1,239, 0 fail |
| `@dopl/desktop-ui` | 46 / 433, 0 fail |
| desktop `node --test` | **3,007 pass, 0 fail** |
| root lint `--max-warnings 0` | clean |
| desktop lint | clean |
| root typecheck | clean |
| `typecheck -w @dopl/desktop-ui` | clean |
| `check-doc-refs` | OK |
| `size-check` (500-line cap) | all within cap |
| the six drift scripts | all ✅ |
| canonical-sets census | 29 / 29 |
| committed-`dist` check | clean after one `build:packages` |

**No flake was hit.** The five known flakes (credits/consume, `shared/version/latest-release`,
`login-form-core`, `agent-authoring`, `billing/credits-link-reroute`) all passed first time in the
full run, so none needed isolation.
