# HOME AGENTS TAB — Implementation Plan

**Repo:** Dopl · branch `master` · verified 2026-08-26. Sibling: `home-knowledge-panels.plan.md`.
The /home Agents tab surfaces AGENT TEMPLATES (the workspace agent-template mechanism) for the home surface. Guests never see this tab (they can't spin up agents) — channel-member-facing only, capability-gated in UI AND floor-gated server-side.

## SAMUEL'S RULINGS (2026-08-26, ruled by the orchestrator while Samuel slept; all conservative, all for morning review)
- **Q1 → visibility axis, NOT channel_resource_grants.** Ship on `agent_templates.visibility`. Inside a container, visibility already collapses to shared(`workspace`)/private, and `agent_only` has no referent for a template (one consumer: main at spawn). A grant table would be a THIRD copy of the visibility matrix (§5A: "written twice and must move together"). Widening is correct only for a MULTI-CHANNEL standard workspace — filed as F-334, not built.
- **Q2 → COPY confirmed.** Cross-container reuse = "Use in this channel": client-composed POST of a NEW container row from the home template's fields, `visibility` forced `private`, **attached knowledgeBases DROPPED** (a home KB isn't in the container; the attach gate would 404 it — do NOT name-match re-attach). Snapshot that diverges, no FK, no sync. No new route.
- **Q3 → guests see NOTHING.** No guest lane for templates. §5A already refuses even a `hasTemplate` boolean to a peer (existence oracle). GUEST_ALLOWED stays 19.
- **Q4 → honour the 2026-08-22 no-concave ruling** on the /home Agents panel (flat `bg-card-surface-subtle`, reuse `template-section.tsx`, never `SectionBox`). Knowledge reads inset, Agents reads flat under one selector — flagged for Samuel's taste; a reversal for visual parity is an explicit ENGINEERING.md entry, not a styling choice. EXTEND the no-concave source sweep to the new /home files.
- **Q5 → peer authorship stays as-is, made VISIBLE** via `template-picker.tsx › authorMarker` on section-A cards. A member-granted claimer can already create templates in your container (POST is member+). Owner-flooring = a route-floor change, filed as a question (Q5), NOT built.
- **Q6 → both "Agents" names live.** /home Agents face = identities; channel info-column Agents tab = running sessions. §5's noun rule ("the noun on both is the AGENT") has tests. Renaming needs Samuel's word — record the collision in §5A + both docblocks, don't rename.

## 0. Corrections to the brief (measured)
0.1 **agent_templates IS APPLIED** (live: version `20260823091848`, join on NAME per F-304; table has 2 rows; `agent_template_teams`/`_knowledge_bases` exist). The dev mock (`client/mock.ts` + `use-agent-templates.ts › isMockFallback`) has discharged its purpose. On /home it's a CORRECTNESS bug: `isMockFallback` fires on `error!=null && data===undefined`, and on a link container a 403/404 is NORMAL (guest opening tab, roster change, stale header) → a dev build would paint 2 fabricated templates under a channel with none. DELETE it in M0 (rewrite the test down to survivors, don't remove whole). File F-332. §5A's three "WRITTEN" bullets (agent_templates, channel_sessions_template_name `20260823092005`, channel_launch_directives_template `20260823092038`) are all applied — record the measurement + command.
0.2 **The launch lane is ALREADY wired into /home.** relationship-record → StandaloneChannelSurface(workspaceId=container) → agents-tab/composer → template-picker(container) → useAgentTemplates(container) → launchAgentOnThread → sessions:launch → template-resolve(container). The Chat face's picker already reads container templates; it's empty only because nothing CREATES a template in a container. This tab is the authoring half — NO bridge/desktop/MCP change for launch. State it in the docblock.
0.3 **A home-workspace template CANNOT launch into a home channel** — `getTemplateById` is workspace-filtered; resolve passes the launch workspace; a scope-C id resolved against a container 404s → REFUSE. Grant table can't fix it (same-workspace trigger). §3's COPY is the answer.
0.4 **Inside a container, `visibility` already answers sharing.** No teams exist (containers have no teams), so visibility collapses to private(creator-only)/workspace(both members + agent token). That IS the shared/not-shared axis. `agent_only` has no template referent. A grant = the fact written twice (three times counting §5A's two).
0.5 🔴 **THE WRITE CACHE PATCH IS A PATH-ONLY PREFIX** — `agentTemplateKeys.list().all === ["/api/agent-templates"]`; all 3 writes in `use-agent-template-writes.ts` patch it; TanStack matches by prefix → EVERY workspace variant patched. Fine for a one-workspace page; BROKEN the moment this tab mounts container+home lists together (a container-created template appears under "across all channels" until cold refetch; delete mirrors). Fix M1: `agentTemplateKeys.list().entry({workspaceId})` (no `query` variants exist on this path, so exactly reproducible). Keep `coldKeys` on create. File F-331, fix same change. Mutation-verify: revert to `.all` → two-list render test red.
0.6 **No-concave ruling (§5A, 2026-08-22) vs SectionBox.** Knowledge panel uses SectionBox (correct — ruling isn't about knowledge). Agents panel lives outside the `template-editor.test.tsx › no concave surfaces` source sweep → could break silently. Honour the ruling (Q4), reuse `template-section.tsx` flat panel, EXTEND the sweep to the new files.

## 1. Model — three scopes in `visibility`
A SHARED-in-channel = container, `visibility==="workspace"` (label "Shared in this channel", NOT "Public" — add `SECTIONS_CONTAINER`/`labelFor(visibility,surface)` in `lib/visibility.ts`, never hand-typed). B PRIVATE-in-channel = container, `private && createdBy===me`. C PRIVATE-across-channels = home workspace, `private && createdBy===me`. Section A alone (who else can wear this); B+C behind a SelectMenu scope pill. `team` is a DEAD value in a container — editor offers TWO options there (filter `SECTIONS`/`VISIBILITY_OPTIONS`, pass `teams={[]}`); a `team` row is DROPPED from sections (groupByVisibility already does this), never mis-filed. Authorship marker on every section-A card (security signal, not decoration).

## 2. Read model — two reads, zero new endpoints
`containerList = useAgentTemplates(channel.workspaceId)` (A+B); `homeList = useAgentTemplates(homeWorkspaceId)` (C, LAZY — behind the pill; add `enabled` to the hook or mount behind useQuery like knowledge-panels; `pendingRow(scopePending)`). Scope C props ride the existing boot query (`identity.data.workspace?.id/.segment/.role`, null until onboarded — handle it). Plain `["/api/agent-templates",<wsId>,undefined]` keys, distinguished by workspace — no channel-scoped key needed (simpler than knowledge). §8: no new response field ships → no key-deleted fixture (say so, don't write a vacuous test). The ONLY key hazard is §0.5.

## 3. Cross-container reuse — COPY (§0.3, Q2)
"Use in this channel" on a scope-C card: read home row → `draftFromTemplate` → clear teamIds, force `visibility=private`, **clear knowledgeBaseIds** → `writes.create.mutateAsync` against the CONTAINER hook. Client-composed over the existing POST — no new route/service/server. Three rules: (1) 🔒 knowledgeBases DROPPED not carried (home KB isn't in container; no name-match re-attach — that's a second weaker attach gate); (2) SNAPSHOT that diverges, no FK/back-pointer/sync (precedent: channel_sessions.template_name culture); (3) goes through the container's useAgentTemplateWrites → §0.5 must land first. Name collides with nothing (templates have no name uniqueness, deliberately — don't append "(copy)").

## 4. UI
4.1 `index.tsx`: (a) `paneToken` gains `agents:` prefix keyed by row (`AGENTS_PANE + rowId`), branch on startsWith in fixed order, delete `shown==="agents"` equality — else the wrong-channel flash; (b) `renderPane` agents branch reads row from `shown` not `selected`. Layout untouched (ONE LAYOUT). Fix the stale "Agents is still an EmptyState" docblock + HomeTab comment.
4.2 New files under pages/home/: `agent-panels.tsx` (sections/pill/create+copy/empty sentences — sibling of knowledge-panels.tsx), `agent-panel-cards.tsx` (card + authorship marker; REUSE `template-section.tsx`/`TemplateCard`, don't fork), `agent-panels.test.tsx`. Split with headroom, not at cap.
4.3 Three DIFFERENT empty sentences, none rendered against unresolved reads; C-unavailable variant when homeWorkspaceId null.
4.4 One caption line per scope (minimal-copy ruling): "in this channel" = "Yours alone until you share it."; "across all channels" = "Yours alone. Use one here to make a copy in this channel."
4.5 Editor reused unchanged, mounted per target workspace: `teams={[]}` (don't fetch), knowledgeBases = target workspace's base list (container = same list the Knowledge tab reads, share the plain-key entry), two visibility options in a container. Keep the team-clearing logic (guards the workspace page).
4.6 **NO launch control** — the authoring surface deliberately omits it (`agent-templates-core.test.tsx › what this page deliberately leaves out`; §5A: a second launch surface fights `resolve`'s singularity). The Chat picker is one selector away. Add the absence-test to the new panel. ⚠ NAMING: info-panel TABS already has "Agents" = running sessions; /home Agents face = identities. Record in §5A + docblocks; don't rename (Q6).

## 5. Deliberately NOT built
No guest lane (Q3). No grant widening (Q1/§7 F-334). No bridge/preload/IPC/MCP change (§0.2). No audience-ceiling work for templates — knowledge M5's ceiling is knowledge-scoped; whether it extends to templates is F-333, decided when M5 lands (an orchestrator listing the operator's own private templates runs on the OPERATOR's credential = operator's own reach, not a peer leak). No container→home copy (symmetric mechanism, asymmetric argument — ask if wanted).

## 6. Tests / green
New `agent-panels.test.tsx`: pane-token both halves (fade starts + outgoing shows previous channel's templates; findBy not getBy); 🔴 the two-workspace cache pin (F-331 — create in container, assert absent from section C; delete home, assert container unchanged; mutation-verify revert-to-`.all`, state count); three empty sentences none against unresolved reads; `team` row dropped not mis-filed; NO launch control (absence test); scope C not fetched until pill asks. Extend `no concave surfaces` sweep to the new files (SPA tree is a separate vitest project — check the sweep can reach; if not, mirrored sweep, said in its docblock). Stay green unchanged: `guest-route-floor.test.ts` (ALLOWED 19), `write-gate-coverage.test.ts` (no new gated route), role-drift, knowledge-drift, `routes.test.tsx` (tab is local state, not a route). Green = re-derive from `grep -n 'run:' ci.yml`; no packages/ or dopl-desktop-app/ change so §13 release gate NOT invoked.

## 7. Findings
F-331 (write-layer prefix cross-contamination — RESOLVED M1 by entry key; carries the measurement). F-332 (dev mock outlived migration — RESOLVED M0 by deletion). F-333 (audience ceiling scope for templates — decision for knowledge M5, not a bug). F-334 (grant widening for a MULTI-CHANNEL standard workspace — records the exact widening: resource_type CHECK + enforce-trigger branch + AFTER-DELETE GC trigger on agent_templates [hard delete, no tombstone, polymorphic id — same gap as `20260807130000` fixed]; move `ChannelGrantLevel`/`ChannelResourceGrant` to `src/shared/` types or hand-mirror). ⚠ F-329 is the knowledge wave's; highest live measured = F-330.

## 8. Docs ritual
INVARIANTS §5A (the /home face: three scopes on visibility, two-workspace read, copy, no-launch restated, Agents-means-two-things, flat-vs-SectionBox; amend the three migration bullets to applied+command). §4A (/home bullet: three faces built; paneToken rule for the third face; delete the "only the placeholder face is keyed by its name" clause). §8 (entry-vs-prefix rule for a two-workspace-one-path surface — the generalizable half of F-331). ENGINEERING 2026-08-26 (why templates share by visibility, knowledge by grant — a template has one consumer, a KB two). FINDINGS F-331..334. check-doc-refs. KB sync.

## 9. Milestones (each green on full §14; Samuel reviews live)
- M0 Ground truth: delete mock + isMockFallback, rewrite the test to survivors, record applied measurement, file F-332. Checkpoint: workspace Agents page renders real rows; forced 403 = plain error state.
- M1 🔴 The cache fix (F-331) FIRST — entry key in all 3 writes, keep coldKeys, two-workspace pin + mutation count, §8. Checkpoint: create in container never appears under "across all channels".
- M2 The pane: paneToken + renderPane; agent-panels + cards; two reads, lazy C, pill, 3 empty sentences, authorship markers, two-option filter, sweep extension. Checkpoint: live vs mock; rapid switch no flash; team row dropped.
- M3 Authoring: editor per scope (teams=[], target KB list, create-follows-pill). Checkpoint: create in container AND home from one surface, right list/workspace.
- M4 "Use in this channel" copy (§3): client-composed, KBs dropped w/ one line, forced private, snapshot sentence. Checkpoint: copy a home template, launch from Chat picker, ROLE block names it.
- M5 Docs/findings/KB sync.
Sequencing: M1←M0; M2←M1; M3←M2; M4←M3; M5 last. Independent of the knowledge wave's M4/M5 (touches none of service-bases/factory/session-profiles/guest lane).

## 10. Open questions remaining for Samuel (beyond the 6 ruled above)
None blocking — all six ruled conservatively. Q4 (visual parity across faces) and Q5 (owner-flooring container writes) and Q6 (rename one Agents) are Samuel's-taste reversals he may make on review; defaults ship as specced.

## Critical files
- apps/desktop-ui/src/pages/home/index.tsx (tab, paneToken, renderPane, boot props)
- apps/desktop-ui/src/pages/home/knowledge-panels.tsx (structural template)
- src/features/agent-templates/hooks/use-agent-template-writes.ts (F-331 — M1 is this file)
- src/features/agent-templates/lib/visibility.ts (wire↔operator vocabulary; container labels here)
- src/features/agent-templates/components/template-section.tsx (flat no-concave panel to reuse)
- src/features/agent-templates/lib/template-draft.ts (draftFromTemplate/draftToCreateBody — §3 copy)
